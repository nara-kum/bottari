<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="user">

	 <!-- 회원가입 -->
    <select id="insertUser" parameterType="com.example.vo.UserVO">
    
    	<![CDATA[
				insert into users 
				values(null, #{id},#{name},#{password},#{email},#{phone},#{birth},now())
         ]]>
    </select>

	 <!-- 로그인 -->
    <select id="selectIdPw" parameterType="com.example.vo.UserVO" resultType="com.example.vo.UserVO">
    
    	<![CDATA[
				select user_no userNo
					, id
					, name
				from users
				where id = #{id}
				and password = #{password}
         ]]>
    </select>


</mapper>