<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="product">



	<!-- 쇼핑몰상품리스트 -->
	<select id="selectList" resultType="com.example.vo.ProductVO">
	<![CDATA[
		select	product_no,
				category_no,
	            title,
	            price,
	            brand,
	            itemimg
		from product
	]]>
	</select>





	<!-- 상품 등록 -->
	<insert id="insert" parameterType="com.example.vo.ProductVO">
    <![CDATA[
        insert into product (category_no, title, price, brand, itemimg, 
                           shipping_yn, shipping_cost, zipcode, address, detail_address)
        values (#{category_no}, #{title}, #{price}, #{brand}, #{itemimg},
                #{shipping_yn}, #{shipping_cost}, #{zipcode}, #{address}, #{detail_address})
    ]]>
    <selectKey keyProperty="product_no" resultType="int" order="AFTER">	
	     <![CDATA[
			select last_insert_id()
         ]]>        
	</selectKey>
    
	</insert>



	<!-- 상세이미지 등록 -->
	<insert id="detailImageInsert" parameterType="com.example.vo.DetailedImageVO">
		<![CDATA[
			insert into detail_image
			values(null, #{product_no}, #{image_URL}, #{turn})
		]]>
	
	</insert>


	<!-- 옵션 등록 -->
	<insert id="productOptionInsert" parameterType="com.example.vo.ProductOptionVO">
		<![CDATA[
			insert into product_option 
			values(null, #{product_no}, #{option_name})
		]]>
		<selectKey keyProperty="option_no" resultType="int" order="AFTER">	
	     <![CDATA[
			select last_insert_id()
         ]]>        
		</selectKey>
    
	
	
	</insert>


	<!-- 디테일 옵션 등록 -->
	<insert id="detailOptionInsert" parameterType="com.example.vo.ProductOptionVO">
		<![CDATA[
			insert into option_detail
			values(null, #{option_no}, #{detailOPtion_name})
		]]>
	
	</insert>



	<!-- 옵션 정보 저장 <insert id="insertOption" parameterType="com.example.vo.ProductTotalVO"> 
		<![CDATA[ insert into `option` (product_no, name, detailOption_no) values 
		(#{product_no}, #{name}, #{detailOption_no}) ]]> </insert> -->

	<!-- 상세 옵션 저장 <insert id="insertDetailOption" parameterType="com.example.vo.ProductTotalVO" 
		useGeneratedKeys="true" keyProperty="detailOption_no"> <![CDATA[ insert into 
		detailOption (name) values (#{name}) ]]> </insert> -->
	<!-- 상세 이미지 저장 <insert id="insertDetailImage" parameterType="com.example.vo.ProductTotalVO"> 
		<![CDATA[ insert into detailed_image (product_no, image_URL, turn) values 
		(#{product_no}, #{image_URL}, #{turn}) ]]> </insert> -->



	<!-- 상품 상세 조회 
	<select id="selectOne" parameterType="int"
		resultType="com.example.vo.ProductVO">
        <![CDATA[
            select  product_no,
                    category_no,
                    title,
                    price,
                    brand,
                    itemimg,
                    shipping_yn,
                    shipping_cost,
                    zipcode,
                    address,
                    detail_address
            from product
            where product_no = #{product_no}
        ]]>
	</select>
	-->


	<!-- 상품 상세 조회 옵션도 
	<select id="selectOne" parameterType="int" resultType="com.example.vo.ProductVO">
        <![CDATA[
	      select distinct
	                p.product_no,
	                p.category_no,
	                p.title,
	                p.price,
	                p.brand,
	                p.itemimg,
	                p.shipping_yn,
	                p.shipping_cost,
	                p.zipcode,
	                p.address,
	                p.detail_address,
	                po.option_no,
	                po.option_name,
	                od.detailOption_no,
	                od.detailOption_name
	            from product p
	            left join product_option po on p.product_no = po.product_no
	            left join option_detail od on po.option_no = od.option_no
	            where p.product_no = #{product_no}
	            order by po.option_no, od.detailOption_no
        ]]>
	</select>
	-->


	<!-- 상품 상세 조회 - 옵션명만 조회 -->
	<select id="selectOne" parameterType="int" resultType="com.example.vo.ProductVO">
	    <![CDATA[
			select distinct
		            p.product_no,
		            p.category_no,
		            p.title,
		            p.price,
		            p.brand,
		            p.itemimg,
		            p.shipping_yn,
		            p.shipping_cost,
		            p.zipcode,
		            p.address,
		            p.detail_address,
		            po.option_no,
		            po.option_name
		        from product p
		        left join product_option po on p.product_no = po.product_no
		        where p.product_no = #{product_no}
		        order by po.option_no
	
	    ]]>
	</select>







</mapper>