<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="calender">
	
	<!-- 캘린더 이벤트 리스트 -->
	<select id="selectList" resultType="com.example.vo.CalenderVO">
		<![CDATA[
			select	event_no,
					user_no,
					DATE_FORMAT(event_date, '%Y-%m-%d') AS event_date,
					event_name,
					event_memo,
					icon_no	as icon_id
			from Calender
		]]>
	</select>
	
	<insert id="insert" >
		<![CDATA[
			insert into Calender
			values(null, #{user_no}, #{event_date}, #{event_name}, #{event_memo}, #{icon_no})
		]]>
	</insert>

	<update id="update">
		<![CDATA[
			update Calender
			set event_name = #{event_name},
				event_memo = #{event_memo},
				icon_no = #{icon_no}
			where event_no = #{event_no}
		]]>
	</update>
	
</mapper>